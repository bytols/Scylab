lado_da_sala = input('qual o tamanho do lado da sala?');

if (lado_da_sala > 0 ) then;
    area_da_sala = lado_da_sala * lado_da_sala;
    printf('a area da sala é %d' , area_da_sala);
    
else;
    
    disp('valor invaldo');
end;
